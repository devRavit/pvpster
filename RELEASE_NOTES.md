# PvPster

이 파일은 CurseForge 페이지에 표시될 **현재 릴리즈의 user-facing 변경사항**입니다.
릴리즈마다 이 파일 내용을 갱신하세요. 전체 히스토리는 [CHANGELOG.md](CHANGELOG.md) 참조.

---

## 정복 컬럼 표시 정합성

- **cap이 안 보이는 캐릭은 괄호 안 누적치도 숨김** — API가 이번 주 정복 한도를 알려주지 않을 때 `9,873 (17,398)` 같은 표기가 의미를 잃어 `9,873`만 표시
- **cap 미노출 캐릭의 색상 fallback** — 이전엔 무조건 빨강이었지만, 이제 누적 8,000 이상이면 파랑(cap 도달), 미만이면 빨강으로 진행도를 시각적으로 구분
